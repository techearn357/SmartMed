<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".SmartMedApp"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartMed"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- Splash Screen -->
        <activity
            android:name=".ui.auth.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SmartMed.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Auth Activities -->
        <activity android:name=".ui.auth.LoginActivity" android:exported="true" />
        <activity android:name=".ui.auth.RegisterActivity" android:exported="true" />
        <activity android:name=".ui.auth.OtpVerificationActivity" android:exported="true" />
        <activity android:name=".ui.auth.ForgotPasswordActivity" android:exported="true" />
        <activity android:name=".ui.auth.ResetPasswordActivity" android:exported="true" />

        <!-- Main Activity -->
        <activity android:name=".ui.main.MainActivity" android:exported="true" />

        <!-- Medicine Activities -->
        <activity android:name=".ui.medicine.AddMedicineActivity" android:exported="true" />
        <activity android:name=".ui.medicine.EditMedicineActivity" android:exported="true" />
        <activity android:name=".ui.medicine.MedicineDetailActivity" android:exported="true" />

        <!-- Prescription Activities -->
        <activity android:name=".ui.prescription.ScanPrescriptionActivity" android:exported="true" />
        <activity android:name=".ui.prescription.PrescriptionConfirmActivity" android:exported="true" />

        <!-- Caregiver Activities -->
        <activity android:name=".ui.caregiver.CaregiverActivity" android:exported="true" />
        <activity android:name=".ui.caregiver.AddCaregiverActivity" android:exported="true" />

        <!-- Settings Activity -->
        <activity android:name=".ui.settings.SettingsActivity" android:exported="true" />

        <!-- Alarm Receiver -->
        <receiver
            android:name=".reminder.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Medicine Action Receiver (Taken/Snooze/Missed from notification) -->
        <receiver
            android:name=".reminder.MedicineActionReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Boot Receiver to restore alarms -->
        <receiver
            android:name=".reminder.BootReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Firebase Messaging Service -->
        <service
            android:name=".reminder.SmartMedFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- File Provider for camera -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
